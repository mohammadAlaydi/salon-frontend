# 🌸 GLOWNOVA Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         GLOWNOVA Frontend                           │
│                    Next.js 14 + TypeScript                          │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
            ┌───────▼────────┐          ┌────────▼─────────┐
            │  Admin Portal  │          │  Public Portal   │
            └───────┬────────┘          └────────┬─────────┘
                    │                            │
        ┌───────────┴───────────┐    ┌──────────┴──────────┐
        │                       │    │                     │
   ┌────▼─────┐        ┌───────▼────▼────┐      ┌────────▼────────┐
   │   Auth   │        │   React Query   │      │   MSW Mocks     │
   │ Context  │        │   (TanStack)    │      │  (Dev/Testing)  │
   └────┬─────┘        └───────┬─────────┘      └────────┬────────┘
        │                      │                         │
        └──────────────────────┼─────────────────────────┘
                               │
                      ┌────────▼─────────┐
                      │   API Client     │
                      │  (lib/apiClient) │
                      └────────┬─────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
         ┌──────▼──────┐  ┌───▼────┐  ┌─────▼──────┐
         │ Auth Tokens │  │ Tenant │  │ Idempotency│
         │  (Bearer)   │  │  Header│  │    Keys    │
         └─────────────┘  └────────┘  └────────────┘
```

---

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           app/ (App Router)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  layout.tsx  ←──  providers.tsx  ←──  msw-init.tsx                 │
│      │                  │                                           │
│      ├─ globals.css     ├─ QueryClientProvider                     │
│      ├─ Inter font      ├─ AuthProvider                            │
│      └─ Playfair font   └─ Toaster                                 │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                        Route Structure                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  /                                                                  │
│  └─ page.tsx (Landing)                                             │
│                                                                     │
│  /admin                                                             │
│  ├─ layout.tsx (AppShell + AuthGuard)                              │
│  ├─ page.tsx (→ /admin/dashboard)                                  │
│  ├─ login/page.tsx (Public)                                        │
│  ├─ dashboard/page.tsx                                              │
│  ├─ appointments/page.tsx                                           │
│  ├─ staff/page.tsx                                                  │
│  ├─ customers/[id]/page.tsx                                         │
│  ├─ services/page.tsx                                               │
│  ├─ reports/page.tsx                                                │
│  ├─ settings/page.tsx                                               │
│  └─ calendar/page.tsx                                               │
│                                                                     │
│  /booking                                                           │
│  ├─ page.tsx (→ /booking/services)                                 │
│  ├─ services/page.tsx (Step 1)                                     │
│  ├─ staff/page.tsx (Step 2)                                        │
│  ├─ time/page.tsx (Step 3)                                         │
│  ├─ details/page.tsx (Step 4)                                      │
│  └─ confirmation/page.tsx (Step 5)                                 │
│                                                                     │
│  /[salonSlug]                                                       │
│  └─ page.tsx (Public Salon Landing)                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Admin Dashboard Flow

```
┌──────────────┐
│   User       │
│  (Admin)     │
└──────┬───────┘
       │
       │ 1. Navigate to /admin/dashboard
       ▼
┌──────────────┐
│ AuthGuard    │◄───── Check isAuthenticated
└──────┬───────┘
       │ ✅ Authenticated
       │
       │ 2. Render DashboardPage
       ▼
┌──────────────────────────────────────────┐
│  Dashboard Component                     │
│                                          │
│  useDailyReport()                        │
│  useUpcomingAppointments()               │
└──────┬───────────────────────────────────┘
       │
       │ 3. React Query fetches data
       ▼
┌──────────────────────────────────────────┐
│  API Client                              │
│                                          │
│  GET /admin/reports/daily                │
│  GET /admin/appointments?upcoming=true   │
│                                          │
│  Headers:                                │
│    Authorization: Bearer {token}         │
│    X-Tenant-ID: {tenantId}               │
└──────┬───────────────────────────────────┘
       │
       │ 4. MSW intercepts (dev) or real API (prod)
       ▼
┌──────────────────────────────────────────┐
│  MSW Handler (mocks/handlers/admin.ts)   │
│                                          │
│  - Filter by tenantId                    │
│  - Calculate metrics from APPOINTMENTS_DB│
│  - Return mock response                  │
└──────┬───────────────────────────────────┘
       │
       │ 5. Response returned
       ▼
┌──────────────────────────────────────────┐
│  React Query                             │
│                                          │
│  - Cache response                        │
│  - Update component state                │
│  - Trigger re-render                     │
└──────┬───────────────────────────────────┘
       │
       │ 6. UI updates
       ▼
┌──────────────────────────────────────────┐
│  Dashboard UI                            │
│                                          │
│  MetricCards (Revenue, Appointments)     │
│  UpcomingAppointments (Today's schedule) │
│  MiniCalendar (Month view)               │
└──────────────────────────────────────────┘
```

### Booking Flow

```
┌──────────────┐
│   Customer   │
│  (Public)    │
└──────┬───────┘
       │
       │ 1. Visit /booking/services
       ▼
┌────────────────────────────────────────────┐
│  ServicesPage                              │
│  usePublicServices()                       │
└──────┬─────────────────────────────────────┘
       │
       │ 2. Select "Signature Haircut"
       │ → /booking/staff?serviceId=svc-1
       ▼
┌────────────────────────────────────────────┐
│  StaffPage                                 │
│  usePublicStaff(serviceId)                 │
└──────┬─────────────────────────────────────┘
       │
       │ 3. Select "Emma Rodriguez"
       │ → /booking/time?serviceId=svc-1&staffId=staff-1
       ▼
┌────────────────────────────────────────────┐
│  TimePage                                  │
│  usePublicAvailability(serviceId, staffId, │
│                        date)               │
└──────┬─────────────────────────────────────┘
       │
       │ 4. Select "10:00 AM on Jan 15"
       │ → /booking/details?serviceId=...&staffId=...&startTime=...
       ▼
┌────────────────────────────────────────────┐
│  DetailsPage                               │
│  useCreatePublicBooking()                  │
│                                            │
│  Form:                                     │
│  - Name: John Doe                          │
│  - Email: john@example.com                 │
│  - Phone: +15551234567                     │
│  - Notes: First visit                      │
└──────┬─────────────────────────────────────┘
       │
       │ 5. Submit form
       │ POST /public/appointments
       │ Headers: { Idempotency-Key: unique-key }
       ▼
┌────────────────────────────────────────────┐
│  MSW Handler (mocks/handlers/public.ts)    │
│                                            │
│  1. Check idempotency cache                │
│  2. Check for scheduling conflicts         │
│  3. Create appointment in APPOINTMENTS_DB  │
│  4. Cache response with idempotency key    │
│  5. Return { id, status: "confirmed" }     │
└──────┬─────────────────────────────────────┘
       │
       │ 6. Mutation success
       │ → /booking/confirmation?id=apt-123&...
       ▼
┌────────────────────────────────────────────┐
│  ConfirmationPage                          │
│                                            │
│  ✅ Booking Confirmed!                     │
│  Reference: apt-123                        │
│  [Download Calendar] [Back to Home]        │
└────────────────────────────────────────────┘
```

---

## State Management Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Application State                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐      ┌──────────────────┐                    │
│  │  AuthContext     │      │  React Query     │                    │
│  │  (Global)        │      │  (Server State)  │                    │
│  ├──────────────────┤      ├──────────────────┤                    │
│  │ - user           │      │ Cache:           │                    │
│  │ - accessToken    │      │  - appointments  │                    │
│  │ - refreshToken   │      │  - staff         │                    │
│  │ - isAuthenticated│      │  - services      │                    │
│  │ - isLoading      │      │  - customers     │                    │
│  │                  │      │  - reports       │                    │
│  │ Methods:         │      │  - salon data    │                    │
│  │ - login()        │      │                  │                    │
│  │ - logout()       │      │ Options:         │                    │
│  │ - refreshTokens()│      │ - staleTime: 5m  │                    │
│  └──────────────────┘      │ - cacheTime: 30m │                    │
│                            │ - refetchOnFocus │                    │
│                            └──────────────────┘                    │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  URL State (Search Params)                           │          │
│  ├──────────────────────────────────────────────────────┤          │
│  │  Booking Flow:                                       │          │
│  │  - serviceId                                         │          │
│  │  - staffId                                           │          │
│  │  - startTime                                         │          │
│  │                                                      │          │
│  │  Appointments Filters:                               │          │
│  │  - from (date)                                       │          │
│  │  - to (date)                                         │          │
│  │  - status                                            │          │
│  │  - staffId                                           │          │
│  │  - q (search)                                        │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Component State (useState)                          │          │
│  ├──────────────────────────────────────────────────────┤          │
│  │  - isModalOpen                                       │          │
│  │  - selectedDate                                      │          │
│  │  - filters                                           │          │
│  │  - editingItem                                       │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow Diagram

```
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ 1. Visit /admin/dashboard (not authenticated)
       ▼
┌──────────────────────────────────┐
│  AuthGuard                       │
│  - Check isAuthenticated: false  │
│  - Store returnUrl: /admin/dashboard
└──────┬───────────────────────────┘
       │
       │ 2. Redirect to /admin/login
       ▼
┌──────────────────────────────────┐
│  LoginPage                       │
│  - Email input                   │
│  - Password input                │
│  - [Sign In] button              │
│  - [Use Demo Account] button     │
└──────┬───────────────────────────┘
       │
       │ 3. Submit credentials
       │ (admin@demo-salon.com / demo123)
       ▼
┌──────────────────────────────────┐
│  AuthContext.login()             │
│  POST /auth/login                │
└──────┬───────────────────────────┘
       │
       │ 4. MSW Handler
       ▼
┌──────────────────────────────────┐
│  mocks/handlers/auth.ts          │
│  - Validate credentials          │
│  - Find user in USERS_DB         │
│  - Generate tokens:              │
│    * accessToken (15min)         │
│    * refreshToken (7 days)       │
│  - Store session in SESSIONS_DB  │
└──────┬───────────────────────────┘
       │
       │ 5. Return tokens + user
       ▼
┌──────────────────────────────────┐
│  AuthContext                     │
│  - Store accessToken             │
│  - Store refreshToken            │
│  - Store user                    │
│  - Set isAuthenticated: true     │
│  - Persist to localStorage       │
└──────┬───────────────────────────┘
       │
       │ 6. Navigate to returnUrl (/admin/dashboard)
       ▼
┌──────────────────────────────────┐
│  AuthGuard                       │
│  - Check isAuthenticated: true ✅│
│  - Allow access                  │
└──────┬───────────────────────────┘
       │
       │ 7. Render protected content
       ▼
┌──────────────────────────────────┐
│  DashboardPage                   │
│  - All API calls include         │
│    Authorization: Bearer {token} │
└──────────────────────────────────┘


Token Refresh Flow (on 401):
───────────────────────────────────

┌──────────────────────────────────┐
│  API Call returns 401            │
└──────┬───────────────────────────┘
       │
       │ 1. Detect 401 error
       ▼
┌──────────────────────────────────┐
│  apiClient.ts                    │
│  - Call POST /auth/refresh       │
│  - Include refreshToken          │
└──────┬───────────────────────────┘
       │
       │ 2. MSW validates refreshToken
       ▼
┌──────────────────────────────────┐
│  mocks/handlers/auth.ts          │
│  - Find session in SESSIONS_DB   │
│  - Generate new accessToken      │
└──────┬───────────────────────────┘
       │
       │ 3. Update tokens
       ▼
┌──────────────────────────────────┐
│  AuthContext                     │
│  - Update accessToken            │
│  - Persist to localStorage       │
└──────┬───────────────────────────┘
       │
       │ 4. Retry original request
       ▼
┌──────────────────────────────────┐
│  API Call succeeds with new token│
└──────────────────────────────────┘
```

---

## MSW Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         MSW (Mock Service Worker)                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Browser Environment                                 │          │
│  │  (mocks/browser.ts)                                  │          │
│  │                                                      │          │
│  │  export const worker = setupWorker(...handlers);     │          │
│  │                                                      │          │
│  │  Started by: app/msw-init.tsx                        │          │
│  │  - Only in development (NODE_ENV === 'development')  │          │
│  │  - Seeds data before starting worker                │          │
│  │  - Shows loading screen during init                 │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Request Handlers                                    │          │
│  │  (mocks/handlers/)                                   │          │
│  │                                                      │          │
│  │  auth.ts:                                            │          │
│  │    - POST /auth/login                                │          │
│  │    - POST /auth/refresh                              │          │
│  │    - POST /auth/logout                               │          │
│  │                                                      │          │
│  │  admin.ts:                                           │          │
│  │    - GET    /admin/appointments                      │          │
│  │    - POST   /admin/appointments                      │          │
│  │    - PATCH  /admin/appointments/:id                  │          │
│  │    - DELETE /admin/appointments/:id                  │          │
│  │    - GET    /admin/staff                             │          │
│  │    - GET    /admin/customers                         │          │
│  │    - GET    /admin/services                          │          │
│  │    - GET    /admin/reports/daily                     │          │
│  │    - GET    /admin/reports/revenue                   │          │
│  │    - GET    /admin/reports/top-services              │          │
│  │    - GET    /admin/salon                             │          │
│  │    - PATCH  /admin/salon                             │          │
│  │                                                      │          │
│  │  public.ts:                                          │          │
│  │    - GET  /public/salons/:slug                       │          │
│  │    - GET  /public/salons/:slug/services              │          │
│  │    - GET  /public/salons/:slug/staff                 │          │
│  │    - GET  /public/services                           │          │
│  │    - GET  /public/staff                              │          │
│  │    - GET  /public/availability                       │          │
│  │    - POST /public/appointments                       │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  In-Memory Database                                  │          │
│  │  (mocks/state.ts + mocks/seed.ts)                    │          │
│  │                                                      │          │
│  │  USERS_DB:           Array<User>                     │          │
│  │  SESSIONS_DB:        Map<refreshToken, Session>      │          │
│  │  APPOINTMENTS_DB:    Array<Appointment>              │          │
│  │  STAFF_DB:           Array<StaffProfile>             │          │
│  │  CUSTOMERS_DB:       Array<Customer>                 │          │
│  │  SERVICES_DB:        Array<Service>                  │          │
│  │  SALONS_DB:          Array<Salon>                    │          │
│  │  IDEMPOTENCY_CACHE:  Map<key, CachedResponse>        │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Utilities                                           │          │
│  │  (mocks/utils/)                                      │          │
│  │                                                      │          │
│  │  tenantResolver.ts:                                  │          │
│  │    - getTenantFromRequest()                          │          │
│  │    - Filter data by tenantId                         │          │
│  │                                                      │          │
│  │  Other helpers:                                      │          │
│  │    - generateId()                                    │          │
│  │    - hashPassword()                                  │          │
│  │    - generateToken()                                 │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Styling Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                     GLOWNOVA Design System                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  app/globals.css                                                    │
│  ├─ @import "tailwindcss"                                           │
│  ├─ @layer base (CSS variables)                                     │
│  │   └─ :root                                                       │
│  │       ├─ Brand colors (--color-primary, etc.)                   │
│  │       ├─ Status colors (--color-success, etc.)                  │
│  │       ├─ Spacing (--space-1 to --space-8)                       │
│  │       ├─ Radiuses (--radius-sm, md, lg)                         │
│  │       ├─ Shadows (--shadow-card, elevated)                      │
│  │       ├─ Motion (--motion-fast, normal, slow)                   │
│  │       └─ shadcn mappings (--background, --foreground, etc.)     │
│  ├─ @theme inline (Tailwind utilities)                             │
│  │   ├─ --color-glownova-primary                                   │
│  │   ├─ --color-glownova-secondary                                 │
│  │   ├─ --color-status-success, error, warning                     │
│  │   └─ shadcn color mappings                                      │
│  └─ @layer base (typography)                                        │
│      ├─ body: font-sans, bg-background, text-foreground            │
│      └─ h1,h2,h3: font-heading                                      │
│                                                                     │
│  tailwind.config.ts                                                 │
│  └─ content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"] │
│                                                                     │
│  app/layout.tsx                                                     │
│  ├─ Import Inter font → --font-inter                               │
│  ├─ Import Playfair Display → --font-playfair                      │
│  └─ Apply to <body>: className={`${inter.variable} ${playfair...}` │
│                                                                     │
│  components/ui/* (shadcn/ui)                                        │
│  ├─ button.tsx → Uses --color-primary, --radius-md                 │
│  ├─ card.tsx → Uses --shadow-card, --radius-md                     │
│  ├─ input.tsx → Uses --color-border, --radius-sm                   │
│  └─ ... all components use design tokens                           │
│                                                                     │
│  Usage in components:                                               │
│  <div className="bg-glownova-primary text-white rounded-lg         │
│                  shadow-card hover:shadow-elevated                 │
│                  transition-all duration-200">                     │
│    <h1 className="font-heading text-3xl">Title</h1>                │
│    <p className="text-sm text-muted-foreground">Description</p>     │
│  </div>                                                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Production Deployment                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Vercel / Netlify / AWS Amplify                      │          │
│  │                                                      │          │
│  │  Build Command:                                      │          │
│  │    npm run build                                     │          │
│  │                                                      │          │
│  │  Start Command:                                      │          │
│  │    npm start                                         │          │
│  │                                                      │          │
│  │  Environment Variables:                              │          │
│  │    NEXT_PUBLIC_API_BASE_URL=https://api.glownova.com│          │
│  │    NEXT_PUBLIC_USE_MOCKS=false                       │          │
│  │    NODE_ENV=production                               │          │
│  └──────────────────────────────────────────────────────┘          │
│                              │                                      │
│                              │                                      │
│                              ▼                                      │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Next.js Server                                      │          │
│  │  - SSR for /[salonSlug] pages                        │          │
│  │  - API routes (if any)                               │          │
│  │  - Static generation for landing pages               │          │
│  └──────────────────────────────────────────────────────┘          │
│                              │                                      │
│                              │                                      │
│                              ▼                                      │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Backend API                                         │          │
│  │  https://api.glownova.com                            │          │
│  │                                                      │          │
│  │  Endpoints:                                          │          │
│  │    /auth/login                                       │          │
│  │    /auth/refresh                                     │          │
│  │    /admin/*                                          │          │
│  │    /public/*                                         │          │
│  │                                                      │          │
│  │  Features:                                           │          │
│  │    - JWT authentication                              │          │
│  │    - Multi-tenancy (X-Tenant-ID header)              │          │
│  │    - Idempotency (Idempotency-Key header)            │          │
│  │    - Rate limiting                                   │          │
│  │    - CORS configuration                              │          │
│  └──────────────────────────────────────────────────────┘          │
│                              │                                      │
│                              │                                      │
│                              ▼                                      │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  Database (PostgreSQL)                               │          │
│  │  - users                                             │          │
│  │  - tenants                                           │          │
│  │  - appointments                                      │          │
│  │  - staff                                             │          │
│  │  - customers                                         │          │
│  │  - services                                          │          │
│  │  - sessions                                          │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  External Services                                   │          │
│  │                                                      │          │
│  │  - SendGrid / Resend (Email notifications)           │          │
│  │  - Twilio (SMS reminders)                            │          │
│  │  - Stripe (Payment processing)                       │          │
│  │  - Cloudinary / S3 (Image uploads)                   │          │
│  │  - Sentry (Error tracking)                           │          │
│  │  - PostHog (Analytics)                               │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Development vs Production

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Development                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  npm run dev                                                        │
│    │                                                                │
│    ├─ Next.js Dev Server (localhost:3000)                          │
│    ├─ MSW Enabled (NEXT_PUBLIC_USE_MOCKS=true)                     │
│    ├─ Hot Module Replacement                                       │
│    ├─ Source maps                                                  │
│    └─ React DevTools                                               │
│                                                                     │
│  API Calls                                                          │
│    │                                                                │
│    ├─ Intercepted by MSW                                           │
│    ├─ Handled by mocks/handlers/*                                  │
│    ├─ Data from in-memory DB                                       │
│    └─ No real backend needed                                       │
│                                                                     │
│  Benefits:                                                          │
│    ✅ Fast iteration                                                │
│    ✅ No backend dependency                                         │
│    ✅ Predictable data                                              │
│    ✅ Easy to test edge cases                                       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         Production                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  npm run build && npm start                                         │
│    │                                                                │
│    ├─ Next.js Production Server                                    │
│    ├─ MSW Disabled (NEXT_PUBLIC_USE_MOCKS=false)                   │
│    ├─ Optimized bundles                                            │
│    ├─ Minified code                                                │
│    └─ SSR/SSG where applicable                                     │
│                                                                     │
│  API Calls                                                          │
│    │                                                                │
│    ├─ Sent to real backend (https://api.glownova.com)              │
│    ├─ JWT authentication                                           │
│    ├─ Real database queries                                        │
│    └─ Production error handling                                    │
│                                                                     │
│  Benefits:                                                          │
│    ✅ Real data                                                     │
│    ✅ Production performance                                        │
│    ✅ Actual user authentication                                    │
│    ✅ Database persistence                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

This architecture diagram provides a comprehensive visual overview of:

1. ✅ **System components** and their relationships
2. ✅ **Data flow** through the application
3. ✅ **Authentication** mechanisms
4. ✅ **MSW integration** for development
5. ✅ **Styling system** architecture
6. ✅ **Deployment** configuration
7. ✅ **Development vs Production** differences

All pieces work together to create a **production-ready, fully functional salon management system**.

